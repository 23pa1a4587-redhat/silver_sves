rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ DEVELOPMENT ONLY - Open access for testing
    // TODO: Implement proper role-based security rules before production
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
